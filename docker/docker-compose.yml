version: '3.8'

services:
  code-server:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ../workspace:/workspace
      - ./config:/home/vscode/.config
    working_dir: /workspace
    restart: unless-stopped
